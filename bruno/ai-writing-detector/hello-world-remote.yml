info:
  name: hello-world-remote
  type: http
  seq: 2

http:
  method: GET
  url: https://localhost:8443/rest/ai/detector/hello-world
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
