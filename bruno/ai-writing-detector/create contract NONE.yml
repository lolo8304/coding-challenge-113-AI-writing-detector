info:
  name: create contract NONE
  type: http
  seq: 7

http:
  method: POST
  url: https://localhost:8443/rest/ai/detector/v1/contracts
  body:
    type: json
    data: |-
      {
        "name": "Lorenz Hänggi",
        "firstName": "Lorenz",
        "lastName": "Hänggi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
